# Nixpacks configuration for Railway
# Ensures pnpm is used instead of npm

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @beyond/api build"]

[start]
cmd = "pnpm --filter @beyond/api start:prod"
